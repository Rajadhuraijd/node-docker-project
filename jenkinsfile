pipeline{
    agent any
    stages {
        stage("checkout scm"){
            steps{
                checkout scm
            }
        }
        stage("test"){
            steps{
                sh 'sudo npm install'
                sh 'npm test'
            }
        }
        stage("Build"){
            steps{
                sh 'sudo npm run build'
            }
        }
  }
}