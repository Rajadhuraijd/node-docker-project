pipeline{
    agent any
    stages {
        stage("checkout scm"){
            steps{
                checkout scm
            }
        }
        stage("test"){
            steps{
                sh 'sudo npm test'
            }
        }
        stage("Build"){
            steps{
                sh 'npm run build'
            }
        }
  }
}
